# Test Inline Exec Skill

Current directory: !`pwd`

Current time: !`date`

Git status: !`cd ~/.openclaw/workspace && git status --short`

Now use this information to describe the current workspace state.
